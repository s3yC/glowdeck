'use client';
import type { WidgetProps } from '@/types';

export default function PomodoroWidget({ widget }: WidgetProps) {
  return <div className="flex items-center justify-center h-full text-white">Pomodoro</div>;
}
