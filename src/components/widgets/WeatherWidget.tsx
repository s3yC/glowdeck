'use client';
import type { WidgetProps } from '@/types';

export default function WeatherWidget({ widget }: WidgetProps) {
  return <div className="flex items-center justify-center h-full text-white">Weather</div>;
}
