'use client';
import type { WidgetProps } from '@/types';

export default function StocksWidget({ widget }: WidgetProps) {
  return <div className="flex items-center justify-center h-full text-white">Stocks</div>;
}
